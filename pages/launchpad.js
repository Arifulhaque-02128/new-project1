import React from 'react';
import Launchpad from '../Components/LaunchPad/Launchpad';

const launchpad = () => {
    return (
        <div>
            <Launchpad />
        </div>
    );
};

export default launchpad;